- name: Commit
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          # 添加所有更改，如果没变化，--allow-empty 会让它继续执行而不报错
          git add data/news.json
          git commit -m "update data" || echo "没有需要提交的更新"
          git push
